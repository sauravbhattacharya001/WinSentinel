<Window x:Class="WinSentinel.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🛡️ WinSentinel — Security Agent"
        Width="1200" Height="800"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBackground}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Navigation Panel -->
        <Border Grid.Column="0" Background="#14142A" BorderBrush="{StaticResource CardBorder}" BorderThickness="0,0,1,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="🛡️ WinSentinel" 
                          FontSize="20" FontWeight="Bold" 
                          Foreground="{StaticResource TextPrimary}"
                          Margin="16,20,16,20" />

                <StackPanel>
                    <Button Content="📊 Dashboard" Style="{StaticResource NavButton}" Click="NavDashboard_Click" />
                    <Button Content="💬 Chat" Style="{StaticResource NavButton}" Click="NavChat_Click" />
                    <Separator Background="{StaticResource CardBorder}" Margin="16,8" />
                    <Button Content="🔥 Firewall" Tag="Firewall" Style="{StaticResource NavButton}" Click="NavAudit_Click" />
                    <Button Content="🔄 Updates" Tag="Updates" Style="{StaticResource NavButton}" Click="NavAudit_Click" />
                    <Button Content="🛡️ Defender" Tag="Defender" Style="{StaticResource NavButton}" Click="NavAudit_Click" />
                    <Button Content="👤 Accounts" Tag="Accounts" Style="{StaticResource NavButton}" Click="NavAudit_Click" />
                    <Button Content="🌐 Network" Tag="Network" Style="{StaticResource NavButton}" Click="NavAudit_Click" />
                    <Button Content="⚙️ Processes" Tag="Processes" Style="{StaticResource NavButton}" Click="NavAudit_Click" />
                    <Button Content="🚀 Startup" Tag="Startup" Style="{StaticResource NavButton}" Click="NavAudit_Click" />
                    <Button Content="💻 System" Tag="System" Style="{StaticResource NavButton}" Click="NavAudit_Click" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Content Area -->
        <Frame Grid.Column="1" x:Name="ContentFrame" NavigationUIVisibility="Hidden" />
    </Grid>
</Window>
