<Page
    x:Class="WinSentinel.App.Views.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource AppBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="24,16" 
                BorderBrush="{StaticResource CardBorder}" 
                BorderThickness="0,0,0,1">
            <TextBlock Text="🛡️ Security Chat" FontSize="20" FontWeight="SemiBold"
                      Foreground="{StaticResource TextPrimary}" />
        </Border>

        <!-- Chat Messages -->
        <ScrollViewer Grid.Row="1" x:Name="ChatScroller" Padding="24,16"
                     VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="MessageList" />
        </ScrollViewer>

        <!-- Quick Actions -->
        <WrapPanel Grid.Row="2" HorizontalAlignment="Center" Margin="24,8">
            <Button Content="🔍 Full Audit" Click="QuickAction_Click" Tag="Run full audit" 
                    Style="{StaticResource QuickButton}" Margin="4" />
            <Button Content="🔥 Firewall" Click="QuickAction_Click" Tag="Check firewall" 
                    Style="{StaticResource QuickButton}" Margin="4" />
            <Button Content="🛡️ Defender" Click="QuickAction_Click" Tag="Check defender" 
                    Style="{StaticResource QuickButton}" Margin="4" />
            <Button Content="📊 Score" Click="QuickAction_Click" Tag="Security score" 
                    Style="{StaticResource QuickButton}" Margin="4" />
            <Button Content="❓ Help" Click="QuickAction_Click" Tag="Help" 
                    Style="{StaticResource QuickButton}" Margin="4" />
        </WrapPanel>

        <!-- Input Area -->
        <Grid Grid.Row="3" Margin="24,8,24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="InputBox" 
                    KeyDown="InputBox_KeyDown"
                    AcceptsReturn="False"
                    Background="#2A2A4A"
                    Foreground="{StaticResource TextPrimary}"
                    BorderBrush="{StaticResource CardBorder}"
                    Padding="8,6"
                    VerticalContentAlignment="Center" />

            <Button Grid.Column="1" x:Name="SendButton"
                    Content="Send" 
                    Style="{StaticResource AccentButton}"
                    Click="SendButton_Click"
                    Margin="8,0,0,0" />
        </Grid>
    </Grid>
</Page>
