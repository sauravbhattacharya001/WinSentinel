# Certs Directory

This directory holds code signing certificates for MSIX packaging.

**Never commit `.pfx` or `.cer` files** — they are in `.gitignore`.

See README.md for instructions on generating a dev certificate.
